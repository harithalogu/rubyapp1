

<div class="row">
  <%= form_for @employee, html: { class: 'form-horizontal' } do |f| %>
    <% if @employee.errors.any? %>
      <div id="error_explanation" style="width=22%">
        <h4><%= pluralize(@employee.errors.count, "error") %> prohibited this employee form from being saved:</h4>
  
        <ul>
        <% @employee.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
<div class="container" >

<p>
<br>
<h3>Enter New Employee information</h3>
</p>
<hr>

<%= form_for @employee, url:{action: "create"} do |f| %>

 <%= render partial: "employees/form", :locals=>{:f => f} %>
 <%= f.submit 'Create', class: 'btn btn-primary' %>
 
</div>
<% end-%>
<% end %>
</div>